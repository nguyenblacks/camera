name: camera_app
description: "Swavoti Camera — computational photography app."
publish_to: 'none'

version: 1.0.1+2

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.1
  image: ^4.5.4
  gal: ^2.3.0
  path_provider: ^2.1.5
  permission_handler: ^12.0.0+1
  flutter_animate: ^4.5.2
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
